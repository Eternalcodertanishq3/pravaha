global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - "rules.yml"

scrape_configs:
  - job_name: "pravaha"
    static_configs:
      - targets: ["pravaha:8000"]
    metrics_path: "/metrics"
    scrape_interval: 5s
